{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "rootDirectory": "backend",
        "startCommand": "export PYTHONPATH=$PYTHONPATH:$(pwd)/app && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    }
}